if pkgcheck libprelude X
then
        menu_begin MENU_PKG_PRELUDE 'Prelude-IDS package options'
        text 'Install prefix for prelude packages' \
             ROCKCFG_PKG_PRELUDE_PREFIX "opt/prelude"

        menu_end
fi

