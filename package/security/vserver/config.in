# --- T2-COPYRIGHT-BEGIN ---
# t2/package/*/vserver/config.in
# Copyright (C) 2004 - 2026 The T2 SDE Project
# Copyright (C) 1998 - 2003 ROCK Linux Project
# SPDX-License-Identifier: GPL-2.0
# --- T2-COPYRIGHT-END ---

if pkgcheck vserver X; then
	# if you are using contexts, you don't want to
	# use standard quota system.
	if  pkgcheck quota X; then
		SDECFG_PKG_VSERVER_QUOTA=1
	fi

block_begin 5
	comment ' '
	comment '- Package Options - VServer'
	if pkgcheck quota .; then
	bool 'Enable Virtual Server per Context Quota Support' SDECFG_PKG_VSERVER_QUOTA 0
	if [ $SDECFG_PKG_VSERVER_QUOTA -eq 1 ]; then
		pkgenable quota
	else
		pkgdisable quota
	fi
	else
		comment '-- quota package not found. can`t use vquota.'
	fi
block_end
fi
