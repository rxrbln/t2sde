# --- T2-COPYRIGHT-NOTE-BEGIN ---
# This copyright note is auto-generated by ./scripts/Create-CopyPatch.
# 
# T2 SDE: package/.../gnupg2/gnupg2.conf
# Copyright (C) 2004 - 2007 The T2 SDE Project
# 
# More information can be found in the files COPYING and README.
# 
# This program is free software; you can redistribute it and/or modify
# it under the terms of the GNU General Public License as published by
# the Free Software Foundation; version 2 of the License. A copy of the
# GNU General Public License can be found in the file COPYING.
# --- T2-COPYRIGHT-NOTE-END ---

if pkginstalled openldap ; then 
	pkgprefix -t openldap 
	var_append configprefix " " 'CFLAGS="-I$( pkgprefix includedir openldap )"'
	var_append configprefix " " 'LDFLAGS="-L$( pkgprefix libdir openldap )"'
fi

# fix shared files with GnuPG-1
hook_add preconf 3 "find . -name Makefile.in -exec sed -ie 's#\(@PACKAGE@\)#\12#g' {} \;"

# tests inf. loop on ppc64, ignore for now
[ "$arch" = powerpc64 ] && var_append makeopt ' ' 'tests=' && var_append makeinstopt ' ' 'tests='

# guide cross compiling
var_append extraconfopt ' ' "--with-gpg-error-prefix=$root/`pkgprefix libgpg-error`"
var_append extraconfopt ' ' "--with-gcrypt-prefix=$root/`pkgprefix libgcrypt`"
var_append extraconfopt ' ' "--with-libassuan-prefix=$root/`pkgprefix libassuan`"
var_append extraconfopt ' ' "--with-ksba-prefix=$root/`pkgprefix libksba`"
var_append extraconfopt ' ' "--with-pth-prefix=$root/`pkgprefix gnupth`"
