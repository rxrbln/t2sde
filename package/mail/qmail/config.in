#!/bin/sh
if pkgcheck qmail X; then
	menu_begin MENU_PKG_QMAIL 'qmail package options'
		comment 'WARNING: If you patch your copy of qmail its at your'
		comment '  own responsabily. This is not GPL, it`s restricted'
		comment '  by it`s author'
		comment ' '

		bool 'Enable LDAP backend' \
			ROCKCFG_PKG_QMAIL_LDAP 0
	menu_end
fi
