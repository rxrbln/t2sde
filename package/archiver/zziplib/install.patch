# --- T2-COPYRIGHT-NOTE-BEGIN ---
# This copyright note is auto-generated by ./scripts/Create-CopyPatch.
# 
# T2 SDE: package/.../zziplib/install.patch
# Copyright (C) 2007 The T2 SDE Project
# 
# More information can be found in the files COPYING and README.
# 
# This patch file is dual-licensed. It is available under the license the
# patched project is licensed under, as long as it is an OpenSource license
# as defined at http://www.opensource.org/ (e.g. BSD, X11) or under the terms
# of the GNU General Public License as published by the Free Software
# Foundation; either version 2 of the License, or (at your option) any later
# version.
# --- T2-COPYRIGHT-NOTE-END ---

--- zziplib-0.13.49/docs/Makefile.in.vanilla	2007-03-29 09:07:10.000000000 +0000
+++ zziplib-0.13.49/docs/Makefile.in	2007-03-29 09:07:32.000000000 +0000
@@ -549,7 +549,7 @@
 install-man3 : manpages.ar
 	$(mkinstalldirs) $(DESTDIR)$(mandir)/man3
 	P=`pwd` ; cd $(DESTDIR)$(mandir)/man3 \
-        && ar x $$P/manpages.ar && chmod 664 *
+        && ar x $$P/manpages.ar
 
 unpack : manpages.ar htmpages.ar
 	(rm -rf _htm && mkdir _htm && cd _htm && ar x ../htmpages.ar)
--- zziplib-0.13.49/zzip/Makefile.in.vanilla	2007-03-29 09:06:55.000000000 +0000
+++ zziplib-0.13.49/zzip/Makefile.in	2007-03-29 09:07:07.000000000 +0000
@@ -690,12 +690,12 @@
 install-exec-local:
 	@ for i in . $(DESTDIR)$(libdir)/libzzip*.so.13 \
         ; do test -d $$i && continue ; lib=`basename "$$i" .so.13` \
-        ; echo "$(DESTDIR)$(libdir): ln -s $$lib.so.13 $$lib.so.10" \
-        ; (cd $(DESTDIR)$(libdir) && ln -s $$lib.so.13 $$lib.so.10) \
-        ; echo "$(DESTDIR)$(libdir): ln -s $$lib.so.13 $$lib.so.11" \
-        ; (cd $(DESTDIR)$(libdir) && ln -s $$lib.so.13 $$lib.so.11) \
-        ; echo "$(DESTDIR)$(libdir): ln -s $$lib.so.13 $$lib.so.12" \
-        ; (cd $(DESTDIR)$(libdir) && ln -s $$lib.so.13 $$lib.so.12) \
+        ; echo "$(DESTDIR)$(libdir): ln -sf $$lib.so.13 $$lib.so.10" \
+        ; (cd $(DESTDIR)$(libdir) && ln -sf $$lib.so.13 $$lib.so.10) \
+        ; echo "$(DESTDIR)$(libdir): ln -sf $$lib.so.13 $$lib.so.11" \
+        ; (cd $(DESTDIR)$(libdir) && ln -sf $$lib.so.13 $$lib.so.11) \
+        ; echo "$(DESTDIR)$(libdir): ln -sf $$lib.so.13 $$lib.so.12" \
+        ; (cd $(DESTDIR)$(libdir) && ln -sf $$lib.so.13 $$lib.so.12) \
 	; done ; true
 
 uninstall-local :
