#!/bin/sh
if [ $prefix_auto = 1 ]; then
	prefix=opt/java
	set_confopt
fi

if ! pkginstalled blackdown-jdk; then
	pkgprefix -t kaffe
	export JAVA_HOME=$root/$( pkgprefix kaffe )
	export JAVAC=$( pkgprefix bindir kaffe )/javac
fi
