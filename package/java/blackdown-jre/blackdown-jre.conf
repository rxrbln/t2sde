#!/bin/sh
# --- T2-COPYRIGHT-NOTE-BEGIN ---
# This copyright note is auto-generated by ./scripts/Create-CopyPatch.
# 
# T2 SDE: package/.../blackdown-jre/blackdown-jre.conf
# Copyright (C) 2004 - 2005 The T2 SDE Project
# Copyright (C) 1998 - 2005 ROCK Linux Project
# 
# More information can be found in the files COPYING and README.
# 
# This program is free software; you can redistribute it and/or modify
# it under the terms of the GNU General Public License as published by
# the Free Software Foundation; version 2 of the License. A copy of the
# GNU General Public License can be found in the file COPYING.
# --- T2-COPYRIGHT-NOTE-END ---

autoextract=0

prefix=opt/java
set_confopt

java_install_opt=""
java_home_id=JRE_HOME
java_home=/$prefix/j2re${ver/-*/}
java_profile_id=jre

# Check if installing needs to be done with or without X11.
if ! pkginstalled xorg; then
	var_append java_install_opt ' ' "--nox11"
fi

java_main() {
	local srctar=

	# The installation depends on the current architecture.
	case $arch in
		x86)	srctar="`match_source_file -p -i586`" ;;
		x86-64)	srctar="`match_source_file -p -amd64`" ;;
	esac
	[ "$srctar" ] || abort "No binary for this architecture detected!"

	# eliminate interactivity and integrity checks.
	cp -v $srctar .
	patch -p0 ${srctar##*/} $confdir/extract-$arch.diff

	# Execute the downloaded binary in the prefix directory.
	( cd $root/$prefix; sh $builddir/${srctar##*/} $java_install_opt )

	# Prepare necessary environment variables.
	cat <<-EOF > $root/etc/profile.d/$java_profile_id
	$java_home_id=$java_home
	export $java_home_id

	PATH=\$PATH:\$$java_home_id/bin
	export PATH
	EOF
}

custmain=java_main
