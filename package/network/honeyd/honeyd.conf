

if [ $prefix_auto = 1 ] ; then
        if [ "$ROCKCFG_PKG_HONEYD_PREFIX" ] ; then
          prefix="$ROCKCFG_PKG_HONEYD_PREFIX"
        else
          prefix="opt/honeyd"
        fi
        set_confopt
fi

