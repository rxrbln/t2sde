if pkgcheck zope X
then
        menu_begin MENU_PKG_ZOPE 'Zope Package Options'
        text 'Install prefix for Zope application server' \
             ROCKCFG_PKG_ZOPE_PREFIX "opt/zope"
	text 'Zope Product Directory' \
	     ROCKCFG_PKG_ZOPE_PRODDIR "$ROCKCFG_PKG_ZOPE_PREFIX/products" 
        menu_end
fi

