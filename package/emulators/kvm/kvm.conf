# --- T2-COPYRIGHT-NOTE-BEGIN ---
# This copyright note is auto-generated by ./scripts/Create-CopyPatch.
# 
# T2 SDE: package/.../kvm/kvm.conf
# Copyright (C) 2004 - 2009 The T2 SDE Project
# 
# More information can be found in the files COPYING and README.
# 
# This program is free software; you can redistribute it and/or modify
# it under the terms of the GNU General Public License as published by
# the Free Software Foundation; version 2 of the License. A copy of the
# GNU General Public License can be found in the file COPYING.
# --- T2-COPYRIGHT-NOTE-END ---

# we need a bios copy for the Mac patches
hook_add prepatch 5 "cp -ar kvm/bios kvm/bios-mac"

# do not build the kmod
hook_add preconf 5 "rm -fv kvm/kernel/configure"

# build and install the Mac BIOS
if pkginstalled bin86 && pkginstalled acpica; then
	hook_add postmake 3 "GCC_WRAPPER_BYPASS=1 make -C kvm/bios-mac"
	hook_add postmake 7 "install kvm/bios-mac/BIOS-bochs-latest $root/$datadir/qemu/bios-mac.bin"
else
	echo_warning "bin86 or acpica not installed, the bios-mac will not be built!"
fi

# our gcc-4.x is not officially supported
var_append confopt ' ' '--disable-gcc-check' # '--with-patched-kernel'

# do not build the kernel part, we build it along the kernel
#var_append makeopt ' ' 'qemu user'
var_append makeinstopt ' ' "kcmd='@#'"

# does not correctly locate libX11 itself
var_append GCC_WRAPPER_INSERT ' ' "-c?:-L`pkgprefix libdir libx11`"
