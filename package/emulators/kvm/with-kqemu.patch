For mixed VT-aware and classic CPUs it is nice to have the
kqemu as fallback option.

  - Rene Rebe <rene@exactcode.de>

--- kvm-83/configure.vanilla	2009-02-04 14:53:05.000000000 +0000
+++ kvm-83/configure	2009-02-04 14:53:15.000000000 +0000
@@ -144,7 +144,6 @@
 
 #configure qemu
 (cd qemu; ./configure --target-list=$target_exec \
-    --disable-kqemu \
     --extra-cflags="-I $PWD/../libkvm $qemu_cflags" \
     --extra-ldflags="-L $PWD/../libkvm $qemu_ldflags" \
     --kerneldir="$libkvm_kerneldir" \
