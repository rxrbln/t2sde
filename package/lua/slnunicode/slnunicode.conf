# --- T2-COPYRIGHT-BEGIN ---
# t2/package/*/slnunicode/slnunicode.conf
# Copyright (C) 2008 - 2025 The T2 SDE Project
# SPDX-License-Identifier: GPL-2.0
# --- T2-COPYRIGHT-END ---

# no Makefile
runmake=0

# inherit $pkglibdir
. $base/package/*/*/lua-conf.in
hook_add inmake 5 '$CC -shared -fPIC *.c -o unicode.so'
hook_add postmake 5 'mkdir -p $root$pkglibdir; install *.so $root$pkglibdir/'
