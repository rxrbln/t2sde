# --- T2-COPYRIGHT-NOTE-BEGIN ---
# T2 SDE: package/*/flac/hotfix-cross.patch
# Copyright (C) 2022 The T2 SDE Project
# 
# This Copyright note is generated by scripts/Create-CopyPatch,
# more information can be found in the files COPYING and README.
# 
# This patch file is dual-licensed. It is available under the license the
# patched project is licensed under, as long as it is an OpenSource license
# as defined at http://www.opensource.org/ (e.g. BSD, X11) or under the terms
# of the GNU General Public License version 2 as used by the T2 SDE.
# --- T2-COPYRIGHT-NOTE-END ---

--- flac-1.4.1/configure.vanilla	2022-10-08 12:21:24.653540299 +0200
+++ flac-1.4.1/configure	2022-10-08 12:21:45.507541805 +0200
@@ -21863,7 +21863,7 @@
 then :
   printf %s "(cached) " >&6
 else $as_nop
-  test "$cross_compiling" = yes &&
+  test "$_cross_compiling" = yes &&
   as_fn_error $? "cannot check for file existence when cross compiling" "$LINENO" 5
 if test -r "$srcdir/man/flac.1"; then
   eval "$as_ac_File=yes"
@@ -21886,7 +21886,7 @@
 then :
   printf %s "(cached) " >&6
 else $as_nop
-  test "$cross_compiling" = yes &&
+  test "$_cross_compiling" = yes &&
   as_fn_error $? "cannot check for file existence when cross compiling" "$LINENO" 5
 if test -r "$srcdir/man/metaflac.1"; then
   eval "$as_ac_File=yes"
