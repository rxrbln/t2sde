#!/bin/bash
#
# ROCK Linux /etc/rc.d/init.d/squid
#
# Desc: Squid - web cache
# Runlevel: 55 rcX
#
# (C) NOV 2001 by René Rebe <rene.rebe@gmx.net>
#

main_begin

    block_begin(start, `Starting squid proxy server.')
        check(`D_prefix/sbin/squid')
    block_end

    block_begin(stop, `Stopping squid proxy server.')
        check(`D_prefix/sbin/squid -k')
    block_end

main_end

