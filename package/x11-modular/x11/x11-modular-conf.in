
if [ $prefix_auto = 1 ] ; then
	prefix="opt/x11"
	set_confopt
fi

var_append GCC_WRAPPER_INSERT " " "-I$root/$includedir -L$root/$libdir"
var_append LD_LIBRARY_PATH ":" "$root/$libdir"
export LD_LIBRARY_PATH

