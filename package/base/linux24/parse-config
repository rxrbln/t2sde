
if [ -f $confdir/postlinux.conf ] ; then
	echo_status "This package contains additional kernel modules build with any kernel."
	echo_status "Remember to rebuild your kernel if you miss the modules!"
fi
