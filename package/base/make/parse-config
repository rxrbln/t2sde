
# add -j to the makeopt - if the package is not flagged to be broken for
# parallel builds ...
[ "$desc_F" = "${desc_D/NOPARALLEL}"  -a "$ROCKCFG_PARALLEL_MAX" ] &&
	var_insert makeopt " " "-j$ROCKCFG_PARALLEL_MAX"

