--- ./scripts/kconfig/conf.c.orig	2003-01-13 17:11:05.000000000 +0100
+++ ./scripts/kconfig/conf.c	2003-01-13 23:20:21.000000000 +0100
@@ -20,6 +20,7 @@
 	ask_all,
 	ask_new,
 	ask_silent,
+	ask_no2mod,
 	set_default,
 	set_yes,
 	set_mod,
@@ -102,6 +103,15 @@
 	line[1] = 0;
 
 	switch (input_mode) {
+	case ask_no2mod:
+		if (type == S_TRISTATE &&
+		    sym_get_tristate_value(sym) == no) {
+			fprintf(stderr, "Setting %s to 'm'.\n", sym->name);
+			line[0] = 'm';
+			line[1] = '\n';
+			line[2] = 0;
+			break;
+		}
 	case ask_new:
 	case ask_silent:
 		if (sym_has_value(sym)) {
@@ -343,6 +353,7 @@
 		case ask_new:
 		case ask_silent:
 		case ask_all:
+		case ask_no2mod:
 			if (is_new)
 				sym->flags |= SYMBOL_NEW;
 			conf_askvalue(sym, menu_get_prompt(def_menu));
@@ -506,6 +517,9 @@
 		case 'm':
 			input_mode = set_mod;
 			break;
+		case 'M':
+			input_mode = ask_no2mod;
+			break;
 		case 'y':
 			input_mode = set_yes;
 			break;
@@ -545,6 +559,7 @@
 		}
 	case ask_all:
 	case ask_new:
+	case ask_no2mod:
 		conf_read(NULL);
 		break;
 	default:
--- ./Makefile.orig	2003-01-13 17:22:23.000000000 +0100
+++ ./Makefile	2003-01-13 23:17:09.000000000 +0100
@@ -191,7 +191,7 @@
 
 noconfig_targets := xconfig menuconfig config oldconfig randconfig \
 		    defconfig allyesconfig allnoconfig allmodconfig \
-		    clean mrproper distclean \
+		    no2modconfig clean mrproper distclean \
 		    help tags TAGS sgmldocs psdocs pdfdocs htmldocs \
 		    checkconfig checkhelp checkincludes
 
@@ -668,6 +668,9 @@
 allmodconfig: scripts/kconfig/conf
 	./scripts/kconfig/conf -m arch/$(ARCH)/Kconfig
 
+no2modconfig: scripts/kconfig/conf
+	./scripts/kconfig/conf -M arch/$(ARCH)/Kconfig
+
 defconfig: scripts/kconfig/conf
 	./scripts/kconfig/conf -d arch/$(ARCH)/Kconfig
 
