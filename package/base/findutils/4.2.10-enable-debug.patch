
Findutils does use the wrong autoconf variable to assign whether or not to
enable debugging - which results in an empty ac_cv_debug variable when
*debug* is present and since this is != no, resulting in enabling debugging
even with --disable-debug or --enable-debug=no.

  Rene Rebe <rene@exactcode.de>

--- findutils-4.2.10/configure.in	2004-12-06 01:45:51.000000000 +0100
+++ findutils-4.2.10-fixed/configure.in	2004-12-10 03:24:15.486659360 +0100
@@ -20,7 +20,7 @@
 
 AC_ARG_ENABLE(debug,
 	AC_HELP_STRING([--enable-debug],[Enable debugging output which is likely to be interesting to people debugging findutils]),
-	[ac_cv_debug=$withval],[ac_cv_debug=no])
+	[ac_cv_debug=$enableval],[ac_cv_debug=no])
 
 AC_MSG_CHECKING([whether debug output should be produced])
 if test x$ac_cv_debug = xno; then
@@ -161,4 +161,4 @@
 	 po/Makefile.in po/Makefile
 	doc/Makefile lib/Makefile
 	)
-dnl intl/Makefile
\ No newline at end of file
+dnl intl/Makefile

--- findutils-4.2.10/configure	2004-12-06 01:46:07.000000000 +0100
+++ findutils-4.2.10-fixed/configure	2004-12-10 03:25:14.717654880 +0100
@@ -1850,7 +1850,7 @@
 # Check whether --enable-debug or --disable-debug was given.
 if test "${enable_debug+set}" = set; then
   enableval="$enable_debug"
-  ac_cv_debug=$withval
+  ac_cv_debug=$enableval
 else
   ac_cv_debug=no
 fi;
--- ./missing.orig	2005-01-18 19:20:33.162506040 +0100
+++ ./missing	2005-01-18 19:20:03.488017248 +0100
@@ -23,6 +23,8 @@
 # configuration script generated by Autoconf, you may include it under
 # the same distribution terms that you use for the rest of that program.
 
+exit 0
+
 if test $# -eq 0; then
   echo 1>&2 "Try \`$0 --help' for more information"
   exit 1
