# --- T2-COPYRIGHT-NOTE-BEGIN ---
# This copyright note is auto-generated by ./scripts/Create-CopyPatch.
# 
# T2 SDE: package/.../squashfs-tools/pkg_linux_pre.conf
# Copyright (C) 2004 - 2005 The T2 SDE Project
# 
# More information can be found in the files COPYING and README.
# 
# This program is free software; you can redistribute it and/or modify
# it under the terms of the GNU General Public License as published by
# the Free Software Foundation; version 2 of the License. A copy of the
# GNU General Public License can be found in the file COPYING.
# --- T2-COPYRIGHT-NOTE-END ---

case $ver in
	2.4*) v=2.4.31 ;;
	2.6*) v=2.6.12 ;;
	*) v=unknown ;;
esac

pkg_sqfs_tempdir=`mktemp -d`
pkg_sqfs_confdir=$base/package/*/squashfs-tools
pkg_sqfs_ver=`sed -n 's,^\[V\] \(.*\)-\(.*\),\1,p' ${pkg_sqfs_confdir}/squashfs-tools.desc`
pkg_sqfs_src=`match_source_file -p squashfs${pkg_sqfs_ver} squashfs-tools`

tar $taropt $pkg_sqfs_src -C $pkg_sqfs_tempdir

pkg_sqfs_lxpatch=$( echo ${pkg_sqfs_tempdir}/squashfs*/linux-${v}/squashfs*-patch )

cat $pkg_sqfs_lxpatch > ${pkg_sqfs_tempdir}/squashfs${pkg_sqfs_ver}-${v}.patch 

echo_status "Including SquashFS (squashfs${pkg_sqfs_ver}-${v}) patch..."
var_append patchfiles ' ' "${pkg_sqfs_tempdir}/squashfs${pkg_sqfs_ver}-${v}.patch"
