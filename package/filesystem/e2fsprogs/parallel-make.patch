
Although the Makefile looks correct and already contains another parallel make
quirk, it does not seem to work for some people. So we work around a bit more
and force the generation of the headers to be done first.

  - Rene Rebe <rene@exactcode.de>

--- ./lib/ext2fs/Makefile.in.orig	2005-02-05 21:48:48.000000000 +0100
+++ ./lib/ext2fs/Makefile.in	2005-02-13 19:12:23.190158416 +0100
@@ -5,6 +5,9 @@
 my_dir = lib/ext2fs
 INSTALL = @INSTALL@
 
+# hack to really really make sure the generated headers are built first -ReneR
+all:: $(srcdir)/ext2_err.h $(srcdir)/ext2_types.h
+
 @MCONFIG@
 
 @DEBUGFS_CMT@DEBUGFS_LIB_OBJS = bb_compat.o cmp_bitmaps.o fileio.o \
