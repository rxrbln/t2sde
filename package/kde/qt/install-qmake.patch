
Do not create a dead symlink, but instead copy the binary ;-)

  - Rene Rebe <rene@exactcode.de>

--- qt-x11-free-3.3.3/qmake/Makefile.unix.orig	2004-11-04 14:51:21.509381616 +0100
+++ qt-x11-free-3.3.3/qmake/Makefile.unix	2004-11-04 14:51:40.648472032 +0100
@@ -46,9 +46,9 @@
 
 install: qmake
 	[ -d @QT_INSTALL_BINS@ ] || mkdir -p @QT_INSTALL_BINS@
-	-cp -P -f @BUILD_PATH@/bin/qmake @QT_INSTALL_BINS@
+	-cp -f @BUILD_PATH@/bin/qmake @QT_INSTALL_BINS@
 	[ -d @QT_INSTALL_DATA@ ] || mkdir -p @QT_INSTALL_DATA@
-	-cp -R -P -f @SOURCE_PATH@/mkspecs @QT_INSTALL_DATA@
+	-cp -R -f @SOURCE_PATH@/mkspecs @QT_INSTALL_DATA@
 
 clean::
 	rm -f $(OBJS) $(QOBJS)
