# --- T2-COPYRIGHT-BEGIN ---
# t2/package/*/hyperscan/no-native-tune.patch
# Copyright (C) 2024 - 2026 The T2 SDE Project
# SPDX-License-Identifier: GPL-2.0 or patched project license
# --- T2-COPYRIGHT-END ---

--- hyperscan-5.4.2/CMakeLists.txt.vanilla	2024-11-28 23:43:43.426666704 +0100
+++ hyperscan-5.4.2/CMakeLists.txt	2024-11-28 23:43:46.980000044 +0100
@@ -182,7 +182,7 @@
         string(REGEX REPLACE "-O[^ ]*" "" CMAKE_CXX_FLAGS_${CONFIG} "${CMAKE_CXX_FLAGS_${CONFIG}}")
     endforeach ()
 
-    if (CMAKE_COMPILER_IS_GNUCC)
+    if (0)
         message(STATUS "gcc version ${CMAKE_C_COMPILER_VERSION}")
         # If gcc doesn't recognise the host cpu, then mtune=native becomes
         # generic, which isn't very good in some cases. march=native looks at
