# --- T2-COPYRIGHT-BEGIN ---
# t2/package/*/swift/hotfix-clang.patch
# Copyright (C) 2026 The T2 SDE Project
# SPDX-License-Identifier: GPL-2.0 or patched project license
# --- T2-COPYRIGHT-END ---

--- swift-swift-6.3.3-RELEASE/stdlib/cmake/modules/AddSwiftStdlib.cmake.vanilla2026-07-10 04:54:50.449909708 +0300
+++ swift-swift-6.3.3-RELEASE/stdlib/cmake/modules/AddSwiftStdlib.cmake	2026-07-10 04:57:50.425488523 +0300
@@ -2152,7 +2152,7 @@
 
   if(NOT SWIFT_BUILD_RUNTIME_WITH_HOST_COMPILER AND NOT BUILD_STANDALONE AND
      NOT SWIFT_PREBUILT_CLANG AND NOT SWIFTLIB_IS_SWIFT_ONLY)
-    list(APPEND SWIFTLIB_DEPENDS clang)
+    #list(APPEND SWIFTLIB_DEPENDS clang)
   endif()
 
   # Turn off implicit import of _Concurrency when building libraries
@@ -2611,7 +2611,7 @@
         INSTALL_BINARY_SWIFTMODULE ${SWIFTLIB_INSTALL_BINARY_SWIFTMODULE}
       )
     if(NOT SWIFT_BUILT_STANDALONE AND NOT "${CMAKE_C_COMPILER_ID}" MATCHES "Clang")
-      add_dependencies(${VARIANT_NAME} clang)
+	    #add_dependencies(${VARIANT_NAME} clang)
     endif()
 
       if(sdk STREQUAL "WINDOWS")

