# --- T2-COPYRIGHT-NOTE-BEGIN ---
# This copyright note is auto-generated by ./scripts/Create-CopyPatch.
# 
# T2 SDE: package/.../apr-util/libsuff.patch
# Copyright (C) 2006 The T2 SDE Project
# 
# More information can be found in the files COPYING and README.
# 
# This patch file is dual-licensed. It is available under the license the
# patched project is licensed under, as long as it is an OpenSource license
# as defined at http://www.opensource.org/ (e.g. BSD, X11) or under the terms
# of the GNU General Public License as published by the Free Software
# Foundation; either version 2 of the License, or (at your option) any later
# version.
# --- T2-COPYRIGHT-NOTE-END ---

--- apr-util-1.2.2/configure	2005-09-27 01:28:49.000000000 +0200
+++ apr-util-1.2.2-t2/configure	2006-03-04 12:25:13.000000000 +0100
@@ -13807,7 +13807,7 @@
       apu_have_gdbm=0
     else
       CPPFLAGS="-I$withval/include"
-      LIBS="-L$withval/lib "
+      LIBS="-L$withval/lib${libsuff} "
 
       echo "$as_me:$LINENO: checking checking for gdbm in $withval" >&5
 echo $ECHO_N "checking checking for gdbm in $withval... $ECHO_C" >&6
@@ -14023,10 +14023,10 @@
       if test "$apu_have_gdbm" != "0"; then
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$withval/lib\""
-    APRUTIL_LDFLAGS="-L$withval/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$withval/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$withval/lib${libsuff}"
   else
-    apr_addto_bugger="-L$withval/lib"
+    apr_addto_bugger="-L$withval/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -14306,7 +14306,7 @@
         ;;
         *)
           NDBM_INC="-I$withval/include"
-          NDBM_LDFLAGS="-L$withval/lib"
+          NDBM_LDFLAGS="-L$withval/lib${libsuff}"
           echo "$as_me:$LINENO: checking checking for ndbm includes in $withval" >&5
 echo $ECHO_N "checking checking for ndbm includes in $withval... $ECHO_C" >&6
         ;;
@@ -14874,7 +14874,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -15402,10 +15402,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -15477,7 +15477,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -16005,10 +16005,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -16080,7 +16080,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -16608,10 +16608,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -16683,7 +16683,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -17211,10 +17211,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -17286,7 +17286,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -17814,10 +17814,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -17889,7 +17889,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -18417,10 +18417,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -18492,7 +18492,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -19020,10 +19020,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -19095,7 +19095,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -19623,10 +19623,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -19721,7 +19721,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -20249,10 +20249,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -20330,7 +20330,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -20858,10 +20858,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -20939,7 +20939,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -21467,10 +21467,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -21548,7 +21548,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -22076,10 +22076,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -22157,7 +22157,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -22685,10 +22685,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -22766,7 +22766,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -23294,10 +23294,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -23375,7 +23375,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -23903,10 +23903,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -23984,7 +23984,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -24512,10 +24512,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -24596,7 +24596,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -25124,10 +25124,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -25199,7 +25199,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -25727,10 +25727,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -25802,7 +25802,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -26330,10 +26330,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -26405,7 +26405,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -26933,10 +26933,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -27008,7 +27008,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -27536,10 +27536,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -27611,7 +27611,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -28139,10 +28139,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -28214,7 +28214,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -28742,10 +28742,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -28817,7 +28817,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -29345,10 +29345,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -29457,7 +29457,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -29985,10 +29985,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -30060,7 +30060,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -30588,10 +30588,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -30663,7 +30663,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -31191,10 +31191,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -31266,7 +31266,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -31794,10 +31794,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -31869,7 +31869,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -32397,10 +32397,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -32472,7 +32472,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -33000,10 +33000,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -33075,7 +33075,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -33603,10 +33603,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -33678,7 +33678,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -34206,10 +34206,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -34304,7 +34304,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -34832,10 +34832,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -34913,7 +34913,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -35441,10 +35441,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -35522,7 +35522,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -36050,10 +36050,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -36131,7 +36131,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -36659,10 +36659,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -36740,7 +36740,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -37268,10 +37268,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -37349,7 +37349,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -37877,10 +37877,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -37958,7 +37958,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -38486,10 +38486,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -38567,7 +38567,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -39095,10 +39095,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -39179,7 +39179,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -39707,10 +39707,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -39782,7 +39782,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -40310,10 +40310,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -40385,7 +40385,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -40913,10 +40913,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -40988,7 +40988,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -41516,10 +41516,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -41591,7 +41591,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -42119,10 +42119,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -42194,7 +42194,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -42722,10 +42722,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -42797,7 +42797,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -43325,10 +43325,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -43400,7 +43400,7 @@
       ;;
       * )
         if test -d $bdb_place; then
-          LDFLAGS="$LDFLAGS -L$bdb_place/lib"
+          LDFLAGS="$LDFLAGS -L$bdb_place/lib${libsuff}"
           CPPFLAGS="$CPPFLAGS -I$bdb_place/include"
         else
           echo "$as_me:$LINENO: checking for Berkeley DB $bdb_version in $bdb_place" >&5
@@ -43928,10 +43928,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib\""
-    APRUTIL_LDFLAGS="-L$found/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$found/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$found/lib${libsuff}"
   else
-    apr_addto_bugger="-L$found/lib"
+    apr_addto_bugger="-L$found/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -44699,7 +44699,7 @@
       apu_have_pgsql=0
     else
       CPPFLAGS="-I$withval/include"
-      LIBS="-L$withval/lib "
+      LIBS="-L$withval/lib${libsuff} "
 
       { echo "$as_me:$LINENO: checking for pgsql in $withval" >&5
 echo "$as_me: checking for pgsql in $withval" >&6;}
@@ -44915,10 +44915,10 @@
       if test "$apu_have_pgsql" != "0"; then
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$withval/lib\""
-    APRUTIL_LDFLAGS="-L$withval/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$withval/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$withval/lib${libsuff}"
   else
-    apr_addto_bugger="-L$withval/lib"
+    apr_addto_bugger="-L$withval/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -45190,10 +45190,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$withval/lib\""
-    APRUTIL_LDFLAGS="-L$withval/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$withval/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$withval/lib${libsuff}"
   else
-    apr_addto_bugger="-L$withval/lib"
+    apr_addto_bugger="-L$withval/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -45930,7 +45930,7 @@
       apu_have_mysql=0
     else
       CPPFLAGS="-I$withval/include"
-      LIBS="-L$withval/lib "
+      LIBS="-L$withval/lib${libsuff} "
 
       { echo "$as_me:$LINENO: checking for mysql in $withval" >&5
 echo "$as_me: checking for mysql in $withval" >&6;}
@@ -46146,10 +46146,10 @@
       if test "$apu_have_mysql" != "0"; then
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$withval/lib\""
-    APRUTIL_LDFLAGS="-L$withval/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$withval/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$withval/lib${libsuff}"
   else
-    apr_addto_bugger="-L$withval/lib"
+    apr_addto_bugger="-L$withval/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -46422,10 +46422,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$withval/lib\""
-    APRUTIL_LDFLAGS="-L$withval/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$withval/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$withval/lib${libsuff}"
   else
-    apr_addto_bugger="-L$withval/lib"
+    apr_addto_bugger="-L$withval/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -46930,7 +46930,7 @@
       apu_have_sqlite3=0
     else
       CPPFLAGS="-I$withval/include"
-      LIBS="-L$withval/lib "
+      LIBS="-L$withval/lib${libsuff} "
 
       { echo "$as_me:$LINENO: checking for sqlite3 in $withval" >&5
 echo "$as_me: checking for sqlite3 in $withval" >&6;}
@@ -47146,10 +47146,10 @@
       if test "$apu_have_sqlite3" != "0"; then
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$withval/lib\""
-    APRUTIL_LDFLAGS="-L$withval/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$withval/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$withval/lib${libsuff}"
   else
-    apr_addto_bugger="-L$withval/lib"
+    apr_addto_bugger="-L$withval/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -47674,7 +47674,7 @@
       apu_have_sqlite2=0
     else
       CPPFLAGS="-I$withval/include"
-      LIBS="-L$withval/lib "
+      LIBS="-L$withval/lib${libsuff} "
 
       { echo "$as_me:$LINENO: checking for sqlite2 in $withval" >&5
 echo "$as_me: checking for sqlite2 in $withval" >&6;}
@@ -47890,10 +47890,10 @@
       if test "$apu_have_sqlite2" != "0"; then
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$withval/lib\""
-    APRUTIL_LDFLAGS="-L$withval/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$withval/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$withval/lib${libsuff}"
   else
-    apr_addto_bugger="-L$withval/lib"
+    apr_addto_bugger="-L$withval/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
@@ -48801,10 +48801,10 @@
 
 
   if test "x$LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting LDFLAGS to \"-L$apu_iconv_dir/lib\""
-    LDFLAGS="-L$apu_iconv_dir/lib"
+    test "x$silent" != "xyes" && echo "  setting LDFLAGS to \"-L$apu_iconv_dir/lib${libsuff}\""
+    LDFLAGS="-L$apu_iconv_dir/lib${libsuff}"
   else
-    apr_addto_bugger="-L$apu_iconv_dir/lib"
+    apr_addto_bugger="-L$apu_iconv_dir/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $LDFLAGS; do
@@ -49224,21 +49224,21 @@
   fi
 
 
-  if test "x$LDFLAGS" = "x-L$apu_iconv_dir/lib"; then
+  if test "x$LDFLAGS" = "x-L$apu_iconv_dir/lib${libsuff}"; then
     test "x$silent" != "xyes" && echo "  nulling LDFLAGS"
     LDFLAGS=""
   else
     apr_new_bugger=""
     apr_removed=0
     for i in $LDFLAGS; do
-      if test "x$i" != "x-L$apu_iconv_dir/lib"; then
+      if test "x$i" != "x-L$apu_iconv_dir/lib${libsuff}"; then
         apr_new_bugger="$apr_new_bugger $i"
       else
         apr_removed=1
       fi
     done
     if test $apr_removed = "1"; then
-      test "x$silent" != "xyes" && echo "  removed \"-L$apu_iconv_dir/lib\" from LDFLAGS"
+      test "x$silent" != "xyes" && echo "  removed \"-L$apu_iconv_dir/lib${libsuff}\" from LDFLAGS"
       LDFLAGS=$apr_new_bugger
     fi
   fi
@@ -49266,10 +49266,10 @@
 
 
   if test "x$APRUTIL_LDFLAGS" = "x"; then
-    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$apu_iconv_dir/lib\""
-    APRUTIL_LDFLAGS="-L$apu_iconv_dir/lib"
+    test "x$silent" != "xyes" && echo "  setting APRUTIL_LDFLAGS to \"-L$apu_iconv_dir/lib${libsuff}\""
+    APRUTIL_LDFLAGS="-L$apu_iconv_dir/lib${libsuff}"
   else
-    apr_addto_bugger="-L$apu_iconv_dir/lib"
+    apr_addto_bugger="-L$apu_iconv_dir/lib${libsuff}"
     for i in $apr_addto_bugger; do
       apr_addto_duplicate="0"
       for j in $APRUTIL_LDFLAGS; do
