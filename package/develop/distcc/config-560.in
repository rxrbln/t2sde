
if pkgcheck distcc X
then
	menu_begin MENU_PKG_DISTCC 'DISTCC (distributed compiler) Options'
		bool 'Distribute compile jobs wherever possible' \
		     ROCKCFG_PKG_DISTCC_USEIT 0
		text 'Remote hosts (node) to be used' \
		     ROCKCFG_PKG_DISTCC_HOSTS "localhost"
	menu_end
fi

