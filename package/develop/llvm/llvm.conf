# --- T2-COPYRIGHT-NOTE-BEGIN ---
# This copyright note is auto-generated by ./scripts/Create-CopyPatch.
# 
# T2 SDE: package/.../llvm/llvm.conf
# Copyright (C) 2009 - 2017 The T2 SDE Project
# 
# More information can be found in the files COPYING and README.
# 
# This program is free software; you can redistribute it and/or modify
# it under the terms of the GNU General Public License as published by
# the Free Software Foundation; version 2 of the License. A copy of the
# GNU General Public License can be found in the file COPYING.
# --- T2-COPYRIGHT-NOTE-END ---

hook_add preconf 5 "mkdir -p objdir; cd objdir"
cmakelists="../CMakeLists.txt"
var_append cmakeopt " " "-DCMAKE_INSTALL_PREFIX=/$prefix" # -DLLVM_LIBDIR_SUFFIX=${libdir##*/lib}
var_append cmakeopt " " "-DLLVM_BUILD_LLVM_DYLIB=ON .."

#var_append extraconfopt ' ' '--disable-bindings' # TODO: config option
[ $arch = powerpc64 ] && var_append GCC_WRAPPER_APPEND ' ' -mminimal-toc

# inject the clang/cfe sub-project
hook_add prepatch 5 "(cd tools; tar $taropt `match_source_file -p cfe`; mv -v cfe*/ clang)"
hook_add postmake 5 "(cd tools/clang; eval $MAKE $makeopt; eval $MAKE $makeinstopt)"

# and the lld sub-project, likewise
hook_add prepatch 6 "(cd tools; tar $taropt `match_source_file -p lld-`; mv -v lld*/ lld)"
#hook_add postmake 6 "(cd tools/lld; eval $MAKE $makeopt; eval $MAKE $makeinstopt)"

# inject actual g++ version dependent system header include path
inject_cxx_search_path() {
	touch config.{c,cc}
	local p="`gcc -v config.c -fsyntax-only 2>&1 | grep -A 4 "<...> search starts" | grep gcc | sed -n '$s/ *//p'`"
	local p2="`gcc -v config.cc -fsyntax-only 2>&1 | grep -A 1 "<...> search starts" | sed -n '$s/ *//p'`"
	local gcc_arch_target="`echo $p | cut -d / -f 5`"
	local dir32="" dir64=""
	[[ "$libdir" == *lib64 ]] && dir32=32
	
	sed -i -e "s,@T2SDE_SYSTEM_INCLUDE_PATH@,$p," \
	       -e "s,@T2SDE_CXX_SYSTEM_INCLUDE_PATH@,$p2," \
	       -e "s,@T2SDE_ARCH_TARGET@,$gcc_arch_target," \
	       -e "s,@T2SDE_DIR32@,$dir32," -e "s,@T2SDE_DIR64@,$dir64," \
	    tools/clang/lib/Frontend/InitHeaderSearch.cpp
}
hook_add preconf 3 "inject_cxx_search_path"
