# --- T2-COPYRIGHT-NOTE-BEGIN ---
# This copyright note is auto-generated by ./scripts/Create-CopyPatch.
# 
# T2 SDE: package/.../gnash/gnash.conf
# Copyright (C) 2006 - 2007 The T2 SDE Project
# 
# More information can be found in the files COPYING and README.
# 
# This program is free software; you can redistribute it and/or modify
# it under the terms of the GNU General Public License as published by
# the Free Software Foundation; version 2 of the License. A copy of the
# GNU General Public License can be found in the file COPYING.
# --- T2-COPYRIGHT-NOTE-END ---

var_append confopt " " "--enable-pthreads"

# xml libs
pkginstalled libxml && var_append extraconfopt " " "--with-libxml-libraries=`pkgprefix libdir libxml`"
pkginstalled libxml && var_append extraconfopt " " "--with-libxml-includes=`pkgprefix includedir libxml`/libxml2"
var_append makeopt " " "LIBXML_LIBS='-lxml2 -lz -lm'"

# work around undefined reference to `sws_getContext' and `sws_scale'
var_append GCC_WRAPPER_INSERT ' ' '-c?:-lswscale'
var_append GCC_WRAPPER_APPEND ' ' '-c?:-lswscale'

# FIXME: more options to consider maybe:
# --enable-sdk-install

# FIXME: updated this and the mozilla patches for libdir != lib (e.g. lib64)
var_append confopt ' ' '--with-plugindir=$root/usr/lib/mozilla/plugins'
