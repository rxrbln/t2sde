# --- T2-COPYRIGHT-NOTE-BEGIN ---
# T2 SDE: package/*/fastfetch/detect-t2.patch
# Copyright (C) 2024 The T2 SDE Project
# 
# This Copyright note is generated by scripts/Create-CopyPatch,
# more information can be found in the files COPYING and README.
# 
# This patch file is dual-licensed. It is available under the license the
# patched project is licensed under, as long as it is an OpenSource license
# as defined at http://www.opensource.org/ (e.g. BSD, X11) or under the terms
# of the GNU General Public License version 2 as used by the T2 SDE.
# --- T2-COPYRIGHT-NOTE-END ---

--- fastfetch-2.8.6/src/logo/builtin.c.vanilla	2024-05-02 09:29:19.407756329 +0200
+++ fastfetch-2.8.6/src/logo/builtin.c	2024-05-02 09:29:47.115998751 +0200
@@ -3915,7 +3915,7 @@
 static const FFlogo T[] = {
     // T2
     {
-        .names = {"T2"},
+        .names = {"T2", "T2 SDE"},
         .lines = FASTFETCH_DATATEXT_LOGO_T2,
         .colors = {
             FF_COLOR_FG_WHITE,
--- fastfetch-2.8.6/src/detection/os/os_linux.c.vanilla	2024-05-02 10:41:36.374082557 +0200
+++ fastfetch-2.8.6/src/detection/os/os_linux.c	2024-05-02 10:47:34.825281738 +0200
@@ -150,7 +150,7 @@
 
         if(os->prettyName.length == 0)
             ffStrbufAppendS(&os->prettyName, "Bedrock Linux");
-        
+
         parseFile("/bedrock"FASTFETCH_TARGET_DIR_ETC"/os-release", os);
 
         if(allRelevantValuesSet(os))
@@ -166,6 +166,8 @@
         return;
 
     parseFile(FASTFETCH_TARGET_DIR_ETC"/lsb-release", os);
+
+    parseFile(FASTFETCH_TARGET_DIR_ETC"/SDE-RELEASE", os);
 }
 
 void ffDetectOSImpl(FFOSResult* os)
--- fastfetch-2.11.0/src/detection/os/os_linux.c.vanilla	2024-05-02 10:50:53.647024433 +0200
+++ fastfetch-2.11.0/src/detection/os/os_linux.c	2024-05-02 10:52:06.598660508 +0200
@@ -168,6 +168,10 @@
     parseFile(FASTFETCH_TARGET_DIR_ETC"/lsb-release", os);
 
     parseFile(FASTFETCH_TARGET_DIR_ETC"/SDE-RELEASE", os);
+    ffStrbufSetS(&os->name, "T2 SDE");
+    ffStrbufSetS(&os->prettyName, "T2 SDE");
+    ffStrbufSetS(&os->id, "t2");
+    ffStrbufSetS(&os->idLike, "t2sde");
 }
 
 void ffDetectOSImpl(FFOSResult* os)
