#!/bin/sh
. /etc/profile # PATH
# on x86* we usually could use Xvesa, otherwise we rather need Xfbdev
if [ -d /sys/class/graphics/fb0 ]; then
	X="Xfbdev"
else
	X="Xvesa -mode 0x117"
fi

xinit /usr/bin/blackbox -- /usr/X11/bin/$X
