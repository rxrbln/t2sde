
if [ "$ROCKCFG_SPARC64_OPT" ] ; then
	opt=""
	case "$ROCKCFG_SPARC_OPT" in
		v9) opt="-mcpu=ultrasparc -mtune=ultrasparc -mno-app-regs" ;;
		v9vis) opt="-mcpu=ultrasparc -mtune=ultrasparc -mvis -mno-app-regs" ;;
	esac
	var_append GCC_WRAPPER_INSERT " " "$opt" 
fi

